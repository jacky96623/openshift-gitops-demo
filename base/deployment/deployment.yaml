apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy
spec:
  template:
    spec:
      containers:
        - name: app
          image: placeholder
          ports:
            - containerPort: 8080
