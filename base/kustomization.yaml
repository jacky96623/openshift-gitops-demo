apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

components:
  - ../components/app-label

images:
  - name: __image__
    newName: argoproj/rollouts-demo
    newTag: green
